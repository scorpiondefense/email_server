# Webmail Local Environment Configuration
# =========================================
#
# Copy this file to .env.local for local development
#
# Usage:
#   cp .env.local.example .env.local
#   npm run dev
#

# JWT secret for session encryption
JWT_SECRET=dev-secret-change-in-production

# Session expiry in seconds (default: 24 hours)
SESSION_EXPIRY=86400

# IMAP connection settings
IMAP_HOST=localhost
IMAP_PORT=993
IMAP_SECURE=true

# SMTP connection settings
SMTP_HOST=localhost
SMTP_PORT=465
SMTP_SECURE=true

# Alternative: Use non-TLS ports for local testing
# IMAP_HOST=localhost
# IMAP_PORT=143
# IMAP_SECURE=false
# SMTP_HOST=localhost
# SMTP_PORT=587
# SMTP_SECURE=false
